>vite
* vite也是前端的构建工具
* 相较于webpack，它采用了不同的运行方式
  * 在开发时，并不会对代码进行打包，因为在开发模式中，基本不会在意对于ie低版本浏览器的兼容
  * 在项目部署的时候才会对代码进行打包，所以vite在开发过程中会非常的块
* 除了开发环境中的速度块，使用vite也非常的方便
* 而且vite在构建依赖的时候，使用了esbuild，他是用go语言写的
  